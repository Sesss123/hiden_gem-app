{
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@types/cors": "^2.8.19",
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "crypto": "^1.0.1",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  },
  "scripts": {
    "build": "tsc",
    "test:rules": "firebase emulators:exec --only firestore --project hidden-gems-rules-test \"node --test test/firestore.rules.test.js\"",
    "test:zero-trust": "npm run build && node --test test/zero_trust_policy.test.js"
  }
}
