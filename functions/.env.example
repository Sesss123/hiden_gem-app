# Copy to the environment-specific Firebase Functions env file/secret store.
# Must match Laravel STEP_UP_HMAC_SECRET and must never be shipped to Flutter.
STEP_UP_HMAC_SECRET=REPLACE_WITH_AT_LEAST_32_RANDOM_BYTES
